"""Leader Assistant — local knowledge/spec assistant.

Filesystem-only (Markdown + YAML + git) knowledge vault with a
surface-agnostic capability layer exposed over REST (FastAPI).
See specs/ for the authoritative specification.
"""

__version__ = "0.1.0"
