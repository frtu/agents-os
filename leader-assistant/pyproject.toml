[project]
name = "leader-assistant"
version = "0.1.0"
description = "An IM assistant that retrieves knowledge and answers project questions"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "claude-agent-sdk>=0.2.116",
    "gradio>=6.20.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.30.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
