# Production mode (npm run prod / npm run build): skip the in-browser mock and
# talk to the real FastAPI backend. Requests to /api are proxied to :8000 in dev.
VITE_USE_MOCKS=false
VITE_API_BASE_URL=/api/v1
VITE_WS_URL=/api/v1/stream
